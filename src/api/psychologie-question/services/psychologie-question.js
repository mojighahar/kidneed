'use strict';

/**
 * psychologie-question service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::psychologie-question.psychologie-question');
